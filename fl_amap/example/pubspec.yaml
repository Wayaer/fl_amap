name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  fl_amap:
    path: ../
  flutter:
    sdk: flutter
  fl_extended: ^0.2.0
  fl_dio: ^0.2.0
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
